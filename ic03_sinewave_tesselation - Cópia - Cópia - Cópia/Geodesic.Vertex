#version 400 core

in vec2 position;

uniform float amp;

out float y;

void main(){
    y = cos((position.x)) * sin((position.y));
    y = y*amp;
    gl_Position = vec4(position.x, y, position.y, 1.0);
}

