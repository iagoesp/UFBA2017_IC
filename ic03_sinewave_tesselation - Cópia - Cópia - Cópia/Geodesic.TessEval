#version 400 core

layout(triangles, equal_spacing, ccw) in;

uniform mat4 P;
uniform mat4 V;
uniform mat4 MVP;

void main(){
    vec4 pos = vec4(0.0);
    for(int i=0;)gl_in[4*j+i].gl_Position;
    gl_Position = MVP * vec4(gl_TessCoord.x, gl_TessCoord.y, gl_TessCoord.z, 1);
}

